<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.torpedo.webservicemanager.dao.UserDAO">
	<resultMap type="User" id="result">
		<result property="id" column="id" />
		<result property="firstName" column="first_name" />
		<result property="lastName" column="last_name" />
		<result property="email" column="email" />
		<result property="gender" column="gender" />
		<result property="ipAddress" column="ip_address" />
		<result property="date" column="date" />
	</resultMap>

	<select id="selectAll" resultMap="result">
		select * from user order by
		date;
	</select>

	<insert id="insert" parameterType="User">
		insert into user
		values(#{id},#{first_name},#{last_name},#{email},#{gender},#{ip_address},#{date});
	</insert>

	<update id="update" parameterType="User">
		update user set
		id=#{id},first_name=#{firstName},last_name=#{lastName},email=#{email},gender=#{gender},ipAddress=#{ip_address},date=#{date}
		where id=#{id};
	</update>

	<delete id="delete" parameterType="int">
		delete from user where
		id=#{id};
	</delete>
</mapper>